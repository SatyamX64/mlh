class Medicine {
  final String name, amount, pricePerTablet;
  final bool morning, noon, night;
  Medicine(
      {this.name,
      this.amount,
      this.pricePerTablet,
      this.morning,
      this.noon,
      this.night});
}
